/**
 * @class UIMenu
 * @extends UIElement
 * Menu. It is hidden by default, and only displayed when certain conditions are met. The menu is a container which can hold buttons, listen options, text, images, and other elements.
 *
 * Animation can be enabled or disabled for when the menu is displayed or hidden. This animation can be the default, or a custom animation.
 *
 * For custom animations: The name to display the animation must be "show", and the name to hide the animation must be "hide".
 *
 *     @example small frame
 *     this.win.find("menu").setVisible(true);
 */

