/**
 * @class UIBitmapFontText
 * @extends UIElement
 * Numerical Labels. Turn the text into images of equal size, then combine them into one large image. Child images will be extracted and assembled based on text content when displayed. Images typically have a PNG formate with transparent background.
 *
 * NOTICE: The width and height of image are required to be divided with no remainder by columns and rows, otherwise the displayment will be abnormal on some browsers.
 */

