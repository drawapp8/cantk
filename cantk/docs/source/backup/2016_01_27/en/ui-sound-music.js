/**
 * @class UISoundMusic
 * @extends UICheckBox
 * Background music. When running, this is the switch that controls the background music. When designing, it is the interface to add background music files.
 *
 * Adding background music files adds them globally. Deleting a background music element won't delete the background music file.
 *
 * playSoundMusic can be called by any element to play the background music.
 *
 */

