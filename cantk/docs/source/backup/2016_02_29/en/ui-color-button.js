/**
 * @class UIColorButton
 * @extends UIElement
 * Color button. Different colors can be set for different states. Can be a rectangle, oval, or circle.
 *
 */

/**
 * @property {Number} roundRadius
 * Element's rounded angle radius. 0 = no rounded angles.
 */

/**
 * @method setRoundStyle
 * Sets element's rounded angle style.
 * @param {String} roundStyle Rounded angle style. 'l' = rounded angles on the left side, 'r' = rounded angles on the right side; 't' = top, 'b' = bottom, 'a' rounded angles on all sides.
 * @return {UIElement} Returns element.
 *
 */

/**
 * @method getRoundStyle
 * Gets element's rounded angle style.
 * @return {UIElement} Returns rounded angle style.
 *
 */

/**
 * @method setActiveFillColor
 * Sets element's active fill color.
 * @param {String} color color.
 * @return {UIElement} Returns element.
 */

/**
 * @class UIColorTile
 * @extends UIColorButton
 * Color block. Can be a rectangle, oval, or circle.
 *
 */

